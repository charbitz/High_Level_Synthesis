The files are in a form so that the High Level Synthesis tool (Catapult by Mentor Graphics) can use them to produce the specific DUT (Graph Coloring).

The tool uses the two following files in order to synthesize and verify the design:
1)	graph_color.h
2)	graph_color_tb